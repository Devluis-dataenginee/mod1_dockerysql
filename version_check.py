import pandas as pd
import sqlalchemy
import pyarrow

print(f"Pandas versión: {pd.__version__}")
print(f"SQLAlchemy versión: {sqlalchemy.__version__}")
print(f"PyArrow versión: {pyarrow.__version__}")